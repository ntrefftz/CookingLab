<!DOCTYPE html>
<html lang="es">
    <body>
        <h1>Catálogo</h1>
        <div id="catalogo">
            <% recetas.forEach(receta => { %>
                <div class="receta">
                    <a href="/receta/?id=<%= receta.id %>"><h2><%= receta.nombre %></h2></a>
                    <p><%= receta.precio %></p>
                </div>
            <% }) %>
        </div>
    </body>